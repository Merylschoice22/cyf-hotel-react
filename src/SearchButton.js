import React from "react";

const SearchButton = props => {
  return (
    <button
      className="btn btn-primary"
      // onClick={props.onSearch}  //is this doing anything?
    >
      Search
    </button>
  );
};

export default SearchButton;
